---
name: Post merge build step

on:
  push:
    branches:
    - master
    tags:
    - '*'
  pull_request: ~
  workflow_dispatch: ~
permissions:
  contents: write
  id-token: write
jobs:
  protobuf-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
      - name: Update npm (OIDC trusted publishing needs npm >= 11.5.1)
        run: npm install -g npm@latest
      - uses: actions/setup-go@v3
      - name: Build
        run: |
          make clean
          make init
          make all
      - name: Commit changes
        # Skip auto-commit when the PR comes from a fork: the action would try to
        # push generated build artifacts back to a branch that only exists in the
        # forked repo, which fails with "fatal: invalid reference". On same-repo
        # PRs and pushes to master/tags the step runs as before.
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: auto build commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check if tag
        id: check_tag
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "is_tag=true" >> $GITHUB_ENV
          else
            echo "is_tag=false" >> $GITHUB_ENV
          fi

      - name: Publish to npm
        if: env.is_tag == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          yarn
          yarn build
          npm publish --provenance --access public
